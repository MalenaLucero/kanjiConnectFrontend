import { ArrayToTextPipe } from './array-to-text.pipe';

describe('ArrayToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
